<div class="ibox collapsed float-e-margins">
    <div class="ibox-title">
        <h5>Sponsor History (<%= @person.sponsor_histories.count %>)</h5>
        <div class="ibox-tools">
            <a class="collapse-link">
                <i class="fa fa-chevron-up"></i>
            </a>
            <a class="close-link">
                <i class="fa fa-times"></i>
            </a>
        </div>
    </div>
    <div class="ibox-content">

        <div class="row">
            <div class="col-lg-12">
                <table class="table table-hover margin bottom">
                    <thead>
                    <tr>
                        <th style="width:80%">Company</th>
                        <th></th>

                    </tr>
                    </thead>
                    <tbody>
                            <% @person.sponsor_histories.order("start_year desc").each do |sponsor_history| %>
                    <tr>
                        <td>
                            <strong><a href="/companies/<%= sponsor_history.company_id%>"><%= sponsor_history.company.name %></a></strong> (<%= sponsor_history.role.name %>)
                            <%= render('shared/edit_sponsor_history_modal', :sponsor_history => sponsor_history)%>
                        </td>
                        <td>
                            <% if sponsor_history.current %>
                              <%= sponsor_history.start_year %>-Present
                            <% else %>
                              <%= sponsor_history.start_year %>-<%= sponsor_history.end_year %>
                            <%end%>
                        </td>
                    </tr>
                    <%end%>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>