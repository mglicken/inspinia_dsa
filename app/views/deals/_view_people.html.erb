
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
    <% @deal.people.order("role_id ASC").each do |person| %>
        <div class="col-lg-2">
            <div class="ibox">
                <div class="ibox-content product-box">

                      <a href="/people/<%= person.id %>">
                        <!-- Make Height 225px if want to look right, need to make it clip -->
                        <img src="<%= person.image_url %>" width = 100%>
                      
                      </a>
                    <div class="product-desc">
                        <% if person.employee == true%>
                        <span class="product-price">
                                    LI
                        </span>
                        <%end %>
                        <small class="text-muted"><%=person.role.name %></small><br>
                        <strong><%=person.name %></strong>
                    </div>
                </div>
            </div>
        </div>
    <%end%>
    </div>
</div>