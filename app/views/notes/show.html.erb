<br>
<div class="row">
  <div class="col-sm-12 col-md-12">
    <div class="ibox-content">
        <h3 class="ibox-title" style="font-size:18px"><%= @note.title %></h3>
      <div class="ibox-body">
        <div class="row">
          <div class="col-md-12">
            <dl class="dl-horizontal" style="font-size:12px">

              <dt><%= @note.date %></dt>
              <% if @note.detail.length <= 1500 %>
                <dd> <address>
                  <%= simple_format(@note.detail) %>
                  </address>                                  
              </dd>
              <% else %>
                <dd> <address>
                    <%= simple_format(@note.detail)[0..1499] %><br>
                    <%= render('shared/content_modal', :content => @note.detail, :title => @note.title)%>
                    </address>                                  
                </dd>
                <% end %>
            </dl>

              <%= render('shared/edit_note_modal', :note => @note)%>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<br>
<div class="row">
  <%= render('shared/add_company_to_note_modal')%>
  <%= render('shared/add_person_to_note_modal')%>
  <%= render('shared/add_sponsor_to_note_modal')%>
</div>
  <br>
<%= render('collapsible_info') %>

<%= render('shared/quick_search_js') %>

