<span style="font:black;"><a data-toggle="modal" href="#Modal_Delete_Deal_Person_<%=deal_person.id %>"><i class="fa fa-times"></i></a></span>
<div class="modal inmodal fade" id="Modal_Delete_Deal_Person_<%=deal_person.id %>" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
          <% if deal_person.errors.any? %>
            <% deal_person.errors.full_messages.each do |message| %>
              <div class="alert alert-dismissable alert-danger">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <%= message %>
              </div>
            <% end %>
          <% end %>
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                <h4 class="modal-title">Delete Deal Person</h4>
                <small class="font-bold"> Remove person from deal team.</small>
            </div>
            <div class="modal-body">

              <strong>Are you sure you want to remove "<%= deal_person.person.name%>"" from <%= deal_person.deal.name %>?</strong> Doing so will permanently remove them, and they will need to be added back manually.
            </div>
            <div class="modal-footer">
              <div class="form-group">
                <button type="button" class="btn btn-white" data-dismiss="modal">Close</button>
                <a type="button" href="/delete_deal_person/<%= deal_person.id %>" class="btn btn-lincoln" style="margin-left:10px;">Remove</a>
              </div>
            </div>
        </div>
    </div>
</div>